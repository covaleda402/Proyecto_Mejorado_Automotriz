# URL del Backend API.
# En desarrollo local: se deja vacío o como /api (Vite hace proxy hacia http://localhost:8080)
# En producción (Vercel): colocar la URL completa de tu backend desplegado (ej. en Render, Railway, etc.):
# VITE_API_URL=https://tu-backend.onrender.com/api
VITE_API_URL=/api
